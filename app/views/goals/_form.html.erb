<%= simple_form_for(@goal) do |f| %>
  <%= f.input :name, placeholder: "Run everyday", label: false %>
  <%= f.association :user, as: :string, input_html: { value: @goal.user.try(:email) } %>
  <%= f.button :submit, "Kick some ass" %>
<% end %>
